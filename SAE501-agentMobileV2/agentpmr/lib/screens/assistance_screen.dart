import 'package:flutter/material.dart';

class AssistanceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Assistance PMR")),
      body: Center(child: Text("Coordination de l'assistance pour les passagers.")),
    );
  }
}
